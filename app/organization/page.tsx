import Link from 'next/link'
import { redirect } from 'next/navigation'
import { createServerSupabaseClient } from '@/lib/supabase-server'
import OrganizationClient from './OrganizationClient'
type Org = { id:string; name:string; kind:string; province:string; description:string; organization_members:{ role:string; status:string }[] | null; organization_teams:{ id:string }[] | null }
export default async function OrganizationPage(){const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login?next=/organization');const {data}=await supabase.from('organizations').select('id,name,kind,province,description,organization_members(role,status),organization_teams(id)').order('created_at',{ascending:false});return <main className="bds-page" style={{minHeight:'100vh',background:'#f7f7f5'}}><header style={{background:'#101827',padding:'14px 18px'}}><Link href="/" style={{color:'white',textDecoration:'none',fontWeight:900}}>BALLDOENSAI.COM</Link></header><section style={{background:'linear-gradient(110deg,#101827,#143f56)',color:'white',padding:'32px 18px'}}><div style={{maxWidth:850,margin:'0 auto'}}><p style={{color:'#f5c518',fontSize:10,fontWeight:900,letterSpacing:1.5,margin:0}}>ORGANIZATION DESK</p><h1 style={{font:'800 clamp(36px,7vw,58px)/.9 var(--font-oswald)',margin:'10px 0'}}>BUILD THE<br/><span style={{color:'#f5c518'}}>NEXT TEAM.</span></h1><p style={{color:'rgba(255,255,255,.7)',fontSize:13}}>จัดการ Academy สโมสร หรือโรงเรียน โดยไม่ปนกับทีมสมัครแข่งขัน</p></div></section><section style={{maxWidth:850,margin:'0 auto',padding:'20px 16px'}}><OrganizationClient organizations={(data??[]) as unknown as Org[]}/></section></main>}
