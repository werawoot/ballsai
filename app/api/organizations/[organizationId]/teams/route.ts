import { NextResponse } from 'next/server'
import { createServerSupabaseClient } from '@/lib/supabase-server'
export async function POST(request:Request,{params}:{params:{organizationId:string}}){const s=await createServerSupabaseClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'กรุณาเข้าสู่ระบบก่อน'},{status:401});const b=await request.json().catch(()=>null) as {name?:string;ageGroup?:string;sport?:string}|null;if(!b?.name?.trim())return NextResponse.json({error:'กรอกชื่อทีม'},{status:400});const {error}=await s.rpc('create_organization_team_safely',{p_organization_id:params.organizationId,p_name:b.name.trim(),p_age_group:b.ageGroup?.trim()??'',p_sport:b.sport==='futsal'?'futsal':'football'});return error?NextResponse.json({error:'สร้างทีมไม่สำเร็จ'},{status:400}):NextResponse.json({ok:true})}
