import Link from 'next/link'
import { redirect } from 'next/navigation'
import { WalletCards, ArrowLeft, Sparkles } from 'lucide-react'
import { createServerSupabaseClient } from '@/lib/supabase-server'
type Wallet={balance:number;total_earned:number;total_spent:number}; type Tx={id:string;amount:number;description:string;created_at:string;transaction_type:string}
export default async function BdsWalletPage(){const s=await createServerSupabaseClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login?next=/bds-wallet');const [{data:wallet},{data:txs}]=await Promise.all([s.from('bds_wallets').select('balance,total_earned,total_spent').eq('user_id',user.id).maybeSingle(),s.from('bds_transactions').select('id,amount,description,created_at,transaction_type').eq('user_id',user.id).order('created_at',{ascending:false}).limit(20)]);const w=(wallet??{balance:0,total_earned:0,total_spent:0}) as Wallet;return <main className="bds-page" style={{minHeight:'100vh',background:'#f7f7f5',paddingBottom:50}}><header style={{background:'#101827',padding:'14px 18px',display:'flex',justifyContent:'space-between'}}><Link href="/profile" style={{color:'white',textDecoration:'none',display:'flex',gap:6,alignItems:'center',fontWeight:800}}><ArrowLeft size={16}/> โปรไฟล์</Link><b style={{color:'#f5c518'}}>BDS WALLET</b></header><section style={{background:'linear-gradient(135deg,#101827,#543b09)',color:'white',padding:'35px 18px'}}><div style={{maxWidth:700,margin:'auto'}}><p style={{color:'#f5c518',fontSize:11,fontWeight:800,letterSpacing:1}}>INTERNAL REWARD CREDITS</p><h1 style={{font:'800 44px/.9 var(--font-oswald)',margin:'6px 0'}}>MY BDS<br/><span style={{color:'#f5c518'}}>WALLET.</span></h1><p style={{color:'rgba(255,255,255,.7)',fontSize:13}}>เครดิตภายใน BallDoenSai ไม่มีมูลค่าเงินและไม่สามารถโอนได้</p></div></section><section style={{maxWidth:700,margin:'-20px auto 0',padding:'0 16px'}}><div style={{background:'#f5c518',borderRadius:16,padding:20,color:'#101827',boxShadow:'0 10px 28px rgba(16,24,39,.2)'}}><span style={{fontSize:12,fontWeight:800}}>CURRENT BALANCE</span><div style={{display:'flex',alignItems:'center',gap:10,marginTop:5}}><WalletCards size={32}/><b style={{font:'800 48px/1 var(--font-oswald)'}}>{w.balance}</b><span style={{fontWeight:900}}>BDS</span></div></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10,margin:'14px 0'}}><div style={{background:'#fff',padding:15,borderRadius:12}}><small>ได้รับทั้งหมด</small><b style={{display:'block',fontSize:24,color:'#167545'}}>{w.total_earned} BDS</b></div><div style={{background:'#fff',padding:15,borderRadius:12}}><small>ใช้ไปทั้งหมด</small><b style={{display:'block',fontSize:24}}>{w.total_spent} BDS</b></div></div><div style={{background:'#fff',borderRadius:14,padding:16}}><h2 style={{margin:0,fontSize:19}}>รายการล่าสุด</h2>{(txs??[]).length===0?<p style={{color:'#6f7b8c',fontSize:13}}>ยังไม่มี BDS Points — รางวัลจะเริ่มแสดงเมื่อมีเหตุการณ์ที่ตรวจสอบได้</p>:<div>{(txs as Tx[]).map(t=><div key={t.id} style={{padding:'12px 0',borderBottom:'1px solid #edf0f2',display:'flex',justifyContent:'space-between'}}><div><b>{t.description}</b><small style={{display:'block',color:'#778395'}}>{new Date(t.created_at).toLocaleDateString('th-TH')}</small></div><b style={{color:t.amount>0?'#167545':'#b42318'}}>{t.amount>0?'+':''}{t.amount} BDS</b></div>)}</div>}</div><p style={{fontSize:11,color:'#758094',display:'flex',gap:6,alignItems:'center'}}><Sparkles size={13}/> BDS v1: ไม่มี crypto, blockchain หรือการแลกเปลี่ยน</p></section></main>}
